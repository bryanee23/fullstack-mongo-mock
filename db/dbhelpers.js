const Product = require('./');
//what is product? need to import documnet
//need to import name aka "groceries"


// complete the dbhelpers
const helpers = {
  getProductsHelper: () => /* (something).find()*/ , // allows me to use promises on controller side
  postProductsHelper: () => , // allows me to use promises on controller side
  updateProductHelper: () => , // allows me to use promises on controller side
  deleteProductHelper: () => , // deleteOne()// allows me to use promises on controller side
};

module.exports = helpers